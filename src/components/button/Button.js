import React from "react";
import "./Button.css";

const Button = () => {
  return (
    <div className="btnContainer">
      <button>Remove</button>
      <button>Add</button>
    </div>
  );
};

export default Button;
